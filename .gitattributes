# Traffic CSVs (peacetime_traffic.csv / attacktime_traffic.csv) are no longer
# stored in Git LFS. They are regenerable intermediate artifacts produced by
# `forward-builder`, git-ignored via `*_traffic.csv`, and not committed.
